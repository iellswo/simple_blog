{% extends "base.html" %}

{% block content %}
{% if list %}
    {% for post, comment in list %}
        <div class="post_header">
            <h2><a href="post/{{ post.id }}">{{ post.title }}</a></h2>
            <div class="posted">posted on {{ post.pub_date }}</div>
        </div>
        <div class="post_body">
            {{ post.content }}
            <div class="comments">
            <a href="post/{{ post.id }}">{{ comment }} comment(s)</a>
            </div>
        </div>
    {% endfor %}
{% else %}
    <p>No posts found..</p>
{%endif %}

{% endblock %}
